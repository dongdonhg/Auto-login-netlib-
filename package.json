{
  "name": "keepalive-netlib",
  "version": "1.0.0",
  "description": "Netlib keepalive",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "install-browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "axios": "latest",
    "playwright": "^1.40.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
